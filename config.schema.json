{
  "$comment": "This schema defines the configuration UI in Zelos App. See https://docs.zeloscloud.io/sdk/how-to/develop-extensions/#configuration for full widget reference.",
  "type": "object",
  "title": "OPAL-RT Extension Configuration",
  "description": "Configure the OPAL-RT extension",
  "properties": {
    "project_path": {
      "type": "string",
      "title": "Project File",
      "description": "Path to the RT-LAB project (.llp) file. Leave blank to auto-connect to the active project.",
      "ui:widget": "file-picker",
      "ui:placeholder": "C:/path/to/model.llp (optional — auto-discovers active project)"
    },
    "poll_interval": {
      "type": "number",
      "title": "Poll Interval (seconds)",
      "description": "Delay between signal reads",
      "minimum": 0.001,
      "maximum": 60.0,
      "default": 1.0,
      "ui:widget": "range"
    },
    "rtlab_path": {
      "type": "string",
      "title": "RT-LAB Install Path",
      "description": "Path to RT-LAB version directory (e.g. C:/OPAL-RT/RT-LAB/v2025.4.7.43). Leave blank for auto-discovery.",
      "ui:widget": "folder-picker",
      "ui:placeholder": "C:/OPAL-RT/RT-LAB/v2025.4.7.43"
    },
    "log_level": {
      "type": "string",
      "title": "Log Level",
      "enum": [
        "DEBUG",
        "INFO",
        "WARNING",
        "ERROR"
      ],
      "default": "INFO"
    }
  }
}
